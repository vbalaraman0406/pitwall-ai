runtime: python312
entrypoint: uvicorn main:app --host 0.0.0.0 --port $PORT

instance_class: F2

automatic_scaling:
  min_instances: 0
  max_instances: 3
  target_cpu_utilization: 0.65

env_variables:
  PYTHONUNBUFFERED: "1"
